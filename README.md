# API Health Monitor

An API health monitoring tool that lets users add public URLs, check their status, and keep a history of previous checks. It sends HTTP requests and records the status code, response time, timestamp, reachability, and errors. It is built with FastAPI, HTTPX, and SQLite.